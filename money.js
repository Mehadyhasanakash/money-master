// calculate button

const button = document.getElementById('calculate-button').addEventListener('click', function () {

    // income input value
    const incomeInput = document.getElementById('income-input');
    const IncomeInputValue = parseFloat(incomeInput.value);

    // clear input
    incomeInput.value = '';







    // fodd input value
    const foodInput = document.getElementById('food-input');
    const foodInputValue = parseFloat(foodInput.value);

    // clear input
    foodInput.value = '';


    // rent-input value
    const rentInput = document.getElementById('rent-input');
    const rentInputValue = parseFloat(rentInput.value);


    // clear input
    rentInput.value = '';


    // clothes-input
    const clothesInput = document.getElementById


    // // expenses-amount total
    // const expensesAmount = document.getElementById('expenses-amount');
    // const expensesToatalAmout = parseFloat(expensesAmount.innerText);
    // // expensesAmount.innerText = expensesToatalAmout;
    // console.log(expensesToatalAmout.innerText);
    // expensesAmount.innerText = foodInputValue;









})